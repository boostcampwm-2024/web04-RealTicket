---
phase: 03-이상-패턴-경계-케이스
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - back/test/booking-abnormal.e2e-spec.ts
autonomous: true
requirements: [ABN-01, ABN-02, ABN-03, ABN-04]

must_haves:
  truths:
    - "ENTERING 상태에서 동일 이벤트 permission 재요청 후 세션 상태가 여전히 ENTERING이다 (ABN-01)"
    - "SELECTING_SEAT 중 다른 이벤트 permission 요청 시 실제 서버 동작이 테스트로 고정된다 (ABN-02)"
    - "setBookingCount 없이 transitionToSelectingSeat 후 좌석 점유 시도 → 400 반환이다 (ABN-03)"
    - "예매 완료 후 동일 이벤트 permission 재요청 → 200 반환이다 (ABN-04)"
  artifacts:
    - path: "back/test/booking-abnormal.e2e-spec.ts"
      provides: "ABN-01~04 describe 블록 포함 이상 패턴 E2E 테스트 파일"
      exports: []
  key_links:
    - from: "back/test/booking-abnormal.e2e-spec.ts"
      to: "back/test/helpers/e2e-setup.ts"
      via: "helpers import"
      pattern: "from './helpers/e2e-setup'"
    - from: "ABN-03 테스트"
      to: "InBookingService.validateAndAddBookedSeat"
      via: "bookSeat → validateAndAddBookedSeat → bookingAmount=0 → BadRequestException"
      pattern: "예약 가능한 좌석 수를 초과했습니다"
---

<objective>
`back/test/booking-abnormal.e2e-spec.ts` 파일을 신규 생성하고 상태 점프·중복 요청 이상 패턴 테스트(ABN-01~04) 4개를 구현한다.

Purpose: 비정상 흐름(중복 permission, 상태 우회, 완료 후 재진입)에 대해 서버가 일관된 응답을 반환함을 회귀 방지 테스트로 고정한다.
Output: `back/test/booking-abnormal.e2e-spec.ts` — ABN-01~04 describe 블록 포함
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-이상-패턴-경계-케이스/03-CONTEXT.md
</context>

<interfaces>
<!-- e2e-setup.ts에서 추출한 헬퍼 시그니처 — executor는 이 계약을 그대로 사용한다 -->

```typescript
// 앱 생성 및 서비스 접근
export async function createTestApp(): Promise<INestApplication>
export function getRedisService(app: INestApplication): TestRedisService

// 인증 헬퍼
export async function loginAsUser(app, loginId: string, loginPassword: string): Promise<string>  // SID 반환
export async function loginAsAdmin(app, loginId?: string, loginPassword?: string): Promise<string>
export function withAuth(req: supertest.Test, sid: string): supertest.Test

// 데이터 생성 헬퍼
export async function createPlace(app, adminSid, overrides?): Promise<number>
export async function createSections(app, adminSid, placeId, sections?): Promise<void>
export async function createProgram(app, adminSid, placeId, overrides?): Promise<number>
export async function createEvent(app, adminSid, programId, overrides?): Promise<number>

// 예매 흐름 헬퍼
export function openEventReservation(app, adminSid, eventId): supertest.Test  // .expect(201)
export function requestPermission(app, sid, eventId): supertest.Test           // .expect(200)
export function setBookingCount(app, sid, bookingAmount): supertest.Test       // .expect(201)
export async function transitionToSelectingSeat(app, sid): Promise<void>       // BookingService.setInBookingFromEntering(sid) 직접 호출
export function bookSeat(app, sid, eventId, sectionIndex, seatIndex, 'reserved'|'deleted'): supertest.Test

// SSE 시뮬레이션 헬퍼
export async function simulateSseDisconnect(app, sid): Promise<void>
export async function simulateSseCloseTimeout(app, sid): Promise<void>
export async function setupSelectingSeat(app, adminSid, eventId, userSid, bookingAmount?): Promise<void>

// 서비스 직접 접근 (상태 검증용)
// app.get(AuthService).getUserSession(sid) → { userStatus: string, targetEvent: number }
// app.get(AuthService).setUserStatusSelectingSeat(sid) → SELECTING_SEAT 직접 전환 (ABN-03 우회용)
```

**ABN-03 핵심 동작 (코드 추적 결과):**
- `setInBookingFromEntering(sid)` → `enterBookingService.getBookingAmount(sid)` → bookingAmount=0 반환 (setBookingCount 없을 때)
- `insertInBooking(eventId, sid, 0)` → in-booking 세션 생성 (bookingAmount=0)
- `authService.setUserStatusSelectingSeat(sid)` → SELECTING_SEAT 진입 성공
- 이후 `bookSeat` → `validateAndAddBookedSeat` → `session.bookingAmount(0) <= session.bookedSeats.length(0)` → `BadRequestException('예약 가능한 좌석 수를 초과했습니다.')` → **HTTP 400**

**ABN-01 핵심 동작:**
- `isAdmission(eventId, sid)` → `authService.setUserEventTarget(sid, eventId)` → `getForwarded(eventId, sid)`
- ENTERING 상태에서 재호출 시 `isInsertable` 결과에 따라 addEnteringSession이 다시 호출될 수 있음
- 상태는 `setUserStatusEntering` 또는 `setUserStatusWaiting`으로 변경
- 이미 entering pool에 있으므로 isInsertable이 false가 될 수도 있음 → 실제 동작을 executor가 확인

**ABN-02 진단 우선 전략:**
- SELECTING_SEAT 중 다른 이벤트 permission 요청 시 `isAdmission` → `setUserEventTarget`이 targetEvent를 덮어씀
- getForwarded 결과로 ENTERING 또는 WAITING 진입 가능 (200 반환 예상)
- executor가 진단 테스트 먼저 실행 후 실제 응답 상태 코드/body로 기대값 고정

**session.guard.ts 403/401 조건:**
- `user:${SID}` 키가 Redis에 없음 → `ForbiddenException` (403)
- 키는 있지만 `USER_LEVEL[userStatus] < USER_LEVEL[requiredStatus]` → `UnauthorizedException` (401)
- WAITING=1, SELECTING_SEAT=3 → WAITING에서 SELECTING_SEAT 가드 통과 불가 → 401
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: booking-abnormal.e2e-spec.ts 파일 생성 + ABN-01/02 describe 블록</name>
  <files>back/test/booking-abnormal.e2e-spec.ts</files>
  <action>
신규 파일 `back/test/booking-abnormal.e2e-spec.ts`를 생성한다.

**파일 상단 구조 (booking-cross-event.e2e-spec.ts와 동일한 패턴):**

```typescript
import { INestApplication } from '@nestjs/common';
import supertest from 'supertest';

import { AuthService } from 'src/auth/service/auth.service';
import { TestRedisService } from 'src/testing/redis/test-redis.service';

import {
  bookSeat,
  createEvent,
  createPlace,
  createProgram,
  createSections,
  createTestApp,
  getRedisService,
  loginAsAdmin,
  loginAsUser,
  openEventReservation,
  requestPermission,
  setBookingCount,
  simulateSseDisconnect,
  simulateSseCloseTimeout,
  transitionToSelectingSeat,
  withAuth,
} from './helpers/e2e-setup';

describe('이상 패턴 & 경계 케이스 (booking-abnormal)', () => {
  let app: INestApplication;
  let redisService: TestRedisService;
  let authService: AuthService;
  let adminSid: string;
  let placeId: number;
  let programId: number;
  let eventId: number;
  let eventId2: number;  // ABN-02 전용 두 번째 이벤트

  beforeAll(async () => {
    app = await createTestApp();
    redisService = getRedisService(app);
    authService = app.get(AuthService);

    adminSid = await loginAsAdmin(app, 'abnadmin1', 'pass1234');
    placeId = await createPlace(app, adminSid);
    await createSections(app, adminSid, placeId, [
      { name: 'A', colLen: 3, seats: [1, 1, 1, 1, 1, 1], order: 0 },
    ]);
    programId = await createProgram(app, adminSid, placeId);
    eventId = await createEvent(app, adminSid, programId, {
      reservationOpenDate: new Date(Date.now() - 86400000).toISOString(),
    });
    eventId2 = await createEvent(app, adminSid, programId, {
      reservationOpenDate: new Date(Date.now() - 86400000).toISOString(),
    });
  });

  beforeEach(async () => {
    await redisService.flushAll();
    adminSid = await loginAsAdmin(app, 'abnadmin1', 'pass1234');
  });

  afterAll(async () => {
    await redisService.flushAll();
    await redisService.disconnect();
    await app.close();
  });

  describe('상태 점프 & 중복 요청 이상 패턴', () => {
    // ABN-01: ENTERING 상태에서 동일 이벤트 permission 재요청
    it('ABN-01: ENTERING 상태에서 중복 permission → 기존 상태 유지', async () => {
      await openEventReservation(app, adminSid, eventId).expect(201);
      const userSid = await loginAsUser(app, 'abn01user1', 'pass1234');

      // 1차 permission → ENTERING 진입
      await requestPermission(app, userSid, eventId).expect(200);
      const sessionBefore = await authService.getUserSession(userSid);
      expect(sessionBefore.userStatus).toBe('ENTERING');

      // 2차 permission (중복 요청) → 200 반환 확인
      await requestPermission(app, userSid, eventId).expect(200);

      // 상태 검증: ENTERING 유지 또는 서버 실제 응답 상태 기록
      const sessionAfter = await authService.getUserSession(userSid);
      // isAdmission → setUserEventTarget → getForwarded 재호출
      // 이미 entering pool에 있을 경우 isInsertable이 false가 돼 WAITING으로 전환될 수 있음
      // → 실행 시 실제 상태 확인 후 아래 단언 고정
      expect(['ENTERING', 'WAITING']).toContain(sessionAfter.userStatus);
      // NOTE: 실행 후 실제 상태 확인, ENTERING이면 toBe('ENTERING')으로 변경
      expect(sessionAfter.targetEvent).toBe(eventId);
    });

    // ABN-02: SELECTING_SEAT 중 다른 이벤트 permission — 진단 먼저
    it('ABN-02: SELECTING_SEAT 중 다른 이벤트 permission → 실제 동작 고정 (진단 후 단언)', async () => {
      await openEventReservation(app, adminSid, eventId).expect(201);
      await openEventReservation(app, adminSid, eventId2).expect(201);
      const userSid = await loginAsUser(app, 'abn02userA', 'pass1234');

      // eventId로 SELECTING_SEAT 진입
      await requestPermission(app, userSid, eventId).expect(200);
      await setBookingCount(app, userSid, 1).expect(201);
      await transitionToSelectingSeat(app, userSid);
      const beforeSession = await authService.getUserSession(userSid);
      expect(beforeSession.userStatus).toBe('SELECTING_SEAT');

      // 다른 이벤트(eventId2) permission 요청 — 실제 응답 확인
      const res = await requestPermission(app, userSid, eventId2);

      // 진단: 응답 상태 코드와 body를 console.log로 출력해 기대값을 고정한다
      // isAdmission → setUserEventTarget(sid, eventId2)로 targetEvent 덮어쓰기 발생 가능
      // 예상: 200 (getForwarded → ENTERING 또는 WAITING)
      // executor가 실행 결과를 확인 후 아래 단언을 확정값으로 변경한다
      console.log('[ABN-02 진단] status:', res.status, 'body:', JSON.stringify(res.body));
      expect([200, 400, 401, 403, 409]).toContain(res.status);

      // 진단 후 기대값 고정 예시 (실행 후 변경):
      // expect(res.status).toBe(200);
      // const afterSession = await authService.getUserSession(userSid);
      // expect(afterSession.targetEvent).toBe(eventId2); // 덮어쓰기 확인
    });
  });
});
```

**주의:** ABN-01과 ABN-02는 진단 모드로 작성한다. executor가 `npm run test:e2e -- --testPathPattern=booking-abnormal`를 실행해 실제 응답을 확인한 후, 해당 파일의 단언을 구체적인 기대값으로 교체한다. 교체 완료 후 다시 테스트 실행해 green 확인.
  </action>
  <verify>
    <automated>cd /c/Users/kxu45/ProgramStudy/naver-boostcamp-membership/GroupProject/web04-RealTicket/back && npm run test:e2e -- --testPathPattern=booking-abnormal 2>&1 | tail -30</automated>
  </verify>
  <done>파일이 생성되고 ABN-01/02 describe 블록이 존재하며, 진단 실행 후 실제 응답에 맞게 단언이 고정된다</done>
</task>

<task type="auto">
  <name>Task 2: ABN-03/04 describe 블록 추가 + 단언 확정</name>
  <files>back/test/booking-abnormal.e2e-spec.ts</files>
  <action>
Task 1에서 생성한 파일의 외부 `describe` 블록 안, ABN-01/02 describe 아래에 ABN-03/04 테스트를 추가한다.

**ABN-03 구현 (setBookingCount 없이 좌석 점유 → 400):**

```typescript
    it('ABN-03: setBookingCount 없이 SELECTING_SEAT 진입 후 좌석 점유 → 400', async () => {
      await openEventReservation(app, adminSid, eventId).expect(201);
      const userSid = await loginAsUser(app, 'abn03user1', 'pass1234');

      // permission → ENTERING (setBookingCount 생략)
      await requestPermission(app, userSid, eventId).expect(200);
      // setBookingCount 호출 없음 → entering:${sid}:temp-booking-amount 없음 → getBookingAmount → 0 반환

      // transitionToSelectingSeat: setInBookingFromEntering(sid)
      // → enterBookingService.getBookingAmount(sid) → 0
      // → insertInBooking(eventId, sid, 0) → in-booking 세션 생성 (bookingAmount=0)
      // → setUserStatusSelectingSeat(sid) → SELECTING_SEAT 진입
      await transitionToSelectingSeat(app, userSid);

      const session = await authService.getUserSession(userSid);
      expect(session.userStatus).toBe('SELECTING_SEAT');

      // 좌석 점유 시도 → validateAndAddBookedSeat → bookingAmount(0) <= bookedSeats.length(0) → 400
      const res = await bookSeat(app, userSid, eventId, 0, 0, 'reserved');
      expect(res.status).toBe(400);

      // 오염 없음 확인: 재진입 가능한지 확인 (이 테스트 후 정상 흐름)
      // 단, beforeEach에서 flushAll이 다음 테스트 전에 정리하므로 추가 검증 생략
    });
```

**ABN-04 구현 (예매 완료 후 동일 이벤트 permission 재요청 → 200):**

```typescript
    it('ABN-04: 예매 완료 후 동일 이벤트 permission 재요청 → 200 반환', async () => {
      await openEventReservation(app, adminSid, eventId).expect(201);
      const userSid = await loginAsUser(app, 'abn04user1', 'pass1234');

      // 예매 완료 흐름
      await requestPermission(app, userSid, eventId).expect(200);
      await setBookingCount(app, userSid, 1).expect(201);
      await transitionToSelectingSeat(app, userSid);
      await bookSeat(app, userSid, eventId, 0, 0, 'reserved').expect(201);

      // /reservation으로 예매 저장
      await withAuth(
        supertest(app.getHttpServer()).post('/reservation'),
        userSid,
      ).send({ eventId, seats: [{ sectionIndex: 0, seatIndex: 0 }] }).expect(201);

      // 예매 완료 후 세션 정리 (in-booking → LOGIN 복귀)
      await simulateSseDisconnect(app, userSid);
      await simulateSseCloseTimeout(app, userSid);

      // 세션 상태 확인: LOGIN
      const sessionAfterCompletion = await authService.getUserSession(userSid);
      expect(sessionAfterCompletion.userStatus).toBe('LOGIN');

      // 동일 이벤트 permission 재요청 → 200 (RESERVED 상태 없음, 재진입 허용)
      const res = await requestPermission(app, userSid, eventId).expect(200);
      expect(res.body).toEqual(
        expect.objectContaining({ enteringStatus: true, waitingStatus: false }),
      );

      // 세션 상태: ENTERING (재진입 성공)
      const sessionAfterReentry = await authService.getUserSession(userSid);
      expect(sessionAfterReentry.userStatus).toBe('ENTERING');
      expect(sessionAfterReentry.targetEvent).toBe(eventId);
    });
```

이 두 it 블록을 `describe('상태 점프 & 중복 요청 이상 패턴', () => { ... })` 안에 ABN-01/02 it 블록 뒤에 추가한다.

**커밋 메시지 형식 (커밋 시 반드시 준수):**
`✅ test: 이상 패턴 ABN-01~04 E2E 테스트 추가 (상태 점프·중복 요청)`
  </action>
  <verify>
    <automated>cd /c/Users/kxu45/ProgramStudy/naver-boostcamp-membership/GroupProject/web04-RealTicket/back && npm run test:e2e -- --testPathPattern=booking-abnormal 2>&1 | tail -30</automated>
  </verify>
  <done>
    - ABN-03: bookSeat → 400 반환 확인
    - ABN-04: 예매 완료 후 requestPermission → 200, sessionAfterReentry.userStatus === 'ENTERING'
    - ABN-01: 실제 응답으로 단언 고정 완료
    - ABN-02: 진단 결과로 단언 고정 완료
    - npm run test:e2e -- --testPathPattern=booking-abnormal: 4개 테스트 모두 pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트→앱 HTTP | supertest로 전송되는 요청, SID 쿠키 위조 시나리오 포함 |
| 앱→Redis | 세션 키 직접 삭제/조작 시나리오 (ABN-06에서 사용, 이 Plan에는 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | session.guard / USER_LEVEL | mitigate | ABN-05에서 WAITING→SELECTING_SEAT 가드 검증 (03-02-PLAN) |
| T-03-02 | Tampering | isAdmission / setUserEventTarget | accept | ABN-02 진단 테스트로 실제 동작 고정, 의도된 덮어쓰기 동작 문서화 |
| T-03-03 | Denial of Service | validateAndAddBookedSeat | mitigate | ABN-03: bookingAmount=0 방어 코드 동작 확인 (400 반환) |
</threat_model>

<verification>
```bash
# ABN-01~04 테스트만 실행
cd back && npm run test:e2e -- --testPathPattern=booking-abnormal

# 전체 회귀 확인 (기존 102개 + 신규 4개 = 106개 이상)
cd back && npm run test:e2e 2>&1 | grep -E "Tests:|Suites:|passed|failed"
```
</verification>

<success_criteria>
- `back/test/booking-abnormal.e2e-spec.ts` 파일 존재
- ABN-01: 단언이 실제 서버 응답으로 고정됨 (ENTERING 또는 WAITING, 실행 후 확정)
- ABN-02: 진단 실행 후 console.log 결과로 단언 고정됨 (200/에러 실제값)
- ABN-03: `bookSeat` → `expect(res.status).toBe(400)` pass
- ABN-04: `requestPermission` → `expect(200)`, `sessionAfterReentry.userStatus === 'ENTERING'` pass
- `npm run test:e2e -- --testPathPattern=booking-abnormal`: 4개 pass, 0 failed
- 전체 회귀: 기존 102개 포함 모든 테스트 green
</success_criteria>

<output>
완료 후 `.planning/phases/03-이상-패턴-경계-케이스/03-01-SUMMARY.md` 생성

포함 내용:
- tasks_completed: 2
- files_changed: back/test/booking-abnormal.e2e-spec.ts
- ABN-02 진단 결과 (실제 응답 상태 코드 및 동작)
- ABN-01 실제 상태 전이 결과
- 커밋 해시
- 전체 테스트 통과 수
</output>
